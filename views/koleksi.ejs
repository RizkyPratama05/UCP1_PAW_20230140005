<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Daftar buku</title>
  <link
    rel="stylesheet"
    href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
  />
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(135deg, #f0f4f8, #d9e4ec);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }

    .container {
      background: white;
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.1);
      max-width: 600px;
      width: 100%;
    }

    h1 {
      font-weight: bold;
      color: #2c3e50;
      margin-bottom: 20px;
    }

    .form-control {
      border-radius: 8px 0 0 8px;
    }

    .btn-primary {
      background: linear-gradient(135deg, #4facfe, #00f2fe);
      border: none;
      border-radius: 0 8px 8px 0;
      font-weight: 500;
      transition: all 0.3s ease;
    }

    .btn-primary:hover {
      background: linear-gradient(135deg, #43e97b, #38f9d7);
      transform: translateY(-1px);
      box-shadow: 0 4px 8px rgba(0,0,0,0.15);
    }

    .list-group-item {
      border-radius: 8px;
      margin-bottom: 10px;
      border: none;
      background: #f9fbfd;
      box-shadow: 0 2px 5px rgba(0,0,0,0.05);
      transition: transform 0.2s ease;
    }

    .list-group-item:hover {
      transform: translateX(3px);
    }

    .completed {
      text-decoration: line-through;
      color: #888;
    }

    .btn-success {
      border-radius: 6px;
    }

    .btn-danger {
      border-radius: 6px;
    }

    .modal-content {
      border-radius: 12px;
    }

    .modal-header {
      background: linear-gradient(135deg, #4facfe, #00f2fe);
      color: white;
      border-top-left-radius: 12px;
      border-top-right-radius: 12px;
    }

    .modal-title {
      font-weight: 600;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="text-center">Daftar Buku</h1>

    <!-- Form to Add New Todo -->
    <form id="addTodoForm" class="my-4">
      <div class="input-group">
        <input
          type="text"
          id="newTask"
          class="form-control"
          placeholder="Add a new judul..."
          required
        />
        <div class="input-group-append">
          <button type="submit" class="btn btn-primary">Add Judul</button>
        </div>
      </div>
    </form>

    <!-- List of Todos -->
    <ul id="koleksi" class="list-group">
      <% perpus.forEach(koleksi=> { %>
      <li class="list-group-item d-flex justify-content-between align-items-center">
        <span class="<%= koleksi.completed ? 'completed' : '' %>">
          <%= koleksi.task %>
        </span>
        <div>
          <button class="btn btn-sm btn-success mr-2 editBtn" data-id="<%= koleksi.id %>">
            Edit
          </button>
          <button class="btn btn-sm btn-danger deleteBtn" data-id="<%= koleksi.id %>">
            Delete
          </button>
        </div>
      </li>
      <% }); %>
    </ul>

    <!-- Edit Todo Modal -->
    <div id="editModal" class="modal fade" tabindex="-1" role="dialog" aria-labelledby="editModalLabel" aria-hidden="true">
      <div class="modal-dialog" role="document">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="editModalLabel">Edit Koleksi</h5>
            <button type="button" class="close text-white" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">&times;</span>
            </button>
          </div>
          <div class="modal-body">
            <form id="editTodoForm">
              <div class="form-group">
                <label for="editJudul">Judul</label>
                <input type="text" id="editTask" class="form-control" required />
              </div>
              <button type="submit" class="btn btn-primary mt-3">
                Save Changes
              </button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
  

  <!-- jQuery -->
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <!-- Bootstrap 4 JS + Popper -->
  <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

  <script>
    $(document).ready(function () {
      let editId;

      // Add new todo
      $("#addKoleksiForm").on("submit", function (e) {
        e.preventDefault();
        const task = $("#newJudul").val().trim();
        if (task) {
          $.ajax({
            url: "/perpus",
            type: "POST",
            contentType: "application/json",
            data: JSON.stringify({ judul: judul }),
            success: function () {
              location.reload();
            },
            error: function (error) {
              console.error("Error adding judul:", error.responseText);
            },
          });
        }
      });

      // Delete a todo
      $(".deleteBtn").on("click", function () {
        const id = $(this).data("id");
        $.ajax({
          url: "/perpus/" + id,
          type: "DELETE",
          success: function () {
            location.reload();
          },
        });
      });

      // Show edit modal
      $(".editBtn").on("click", function () {
        editId = $(this).data("id");
        const currentTask = $(this).closest("li").find("span").text();
        $("#editJudul").val(currentTask);
        $("#editModal").modal("show");
      });

      // Update todo
      $("#editKoleksiForm").on("submit", function (e) {
        e.preventDefault();
        const task = $("#editJudul").val().trim();
        if (task) {
          $.ajax({
            url: "/perpus/" + editId,
            type: "PUT",
            data: JSON.stringify({ judul: judul, completed: false }),
            contentType: "application/json",
            success: function () {
              location.reload();
            },
          });
        }
      });
    });
  </script>
</body>
</html>
